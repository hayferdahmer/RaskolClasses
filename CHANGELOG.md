# Changelog

Все значимые изменения RaskolClasses документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru-RU/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [1.3.2] - 2026-08-30 — «Core-адаптер»

### Добавлено
- **RaskolCore-хук:** `RaskolCoreHook` изолирован через отражение по
  класслоадеру Core-плагина (Bukkit-класслоадеры изолированы, прямой
  `Class.forName` не работает). Когда Core доступен — класс читается из
  паспорта (`RaskolCoreAPI.passportOf(uuid).playerClass()`), иначе —
  LP-фолбэк. Без Core: один инфо-лог, без падения.
- **`/rc debug`** показывает «Источник класса: core/lp/off» — диагностика,
  откуда пришёл класс игрока.
- **Флаг `hooks.raskolcore.enabled`** — выключает хук без пересборки.
- `softdepend += RaskolCore` в `plugin.yml`.

### Изменено
- `PlayerClass.fromCoreId(String)` — отдельный маппинг сырых id паспорта
  («warrior»/«mage»/...), не пересекающийся с `fromLuckPermsGroup`
  (префикс `class_`).
- `ClassProvider.getClassOf` — Core first, LP fallback; кэш общий.

## [1.3.1] - 2026-08-30 — «Прицельное благословение»

### Добавлено
- **Таргет-касты жреца:** ЛКМ со свитком точечной способности (Малое/Быстрое
  исцеление, Слово силы: Щит) по игроку = каст в цель; атака отменяется.
  ЛКМ по мобу — обычная атака (свиток не мешает PvE).
- **Фидбек таргет-кастов:** actionbar кастеру («✚ ник: +N HP», «Щит на: ник»),
  сообщение цели («ник исцелил тебя», «ник наложил на тебя щит»),
  «Цель здорова» при попытке лечения полного HP.
- **Благодать на исходящий хил:** ×1.15 к любому прямому лечению жреца
  (раньше пассивка считала только входящее лечение).
- Флаг `target-cast.enabled` (выключает ЛКМ-таргетинг без пересборки).

### Изменено
- **Жрец:** убран авто-таргетинг лучом взгляда у точечных способностей —
  ПКМ со свитком и `/rc` теперь **всегда в себя**, цель выбирается явно ЛКМ.
  AoE-способности (Круг молитвы, Кара) не изменились.

## [1.3.0] - 2026-08-29 — «Видимость и дисциплина»

### Добавлено
- **Видимость пассивок:** лор карточки класса в `/rc menu` показывает пассивки с описаниями,
  строки пассивок в `/rc`, числа пассивок (chance/multiplier/threshold/КД) в `/rc debug`.
- **Описания способностей:** 21 активка и 6 пассивок имеют поле `description` в конфиге с
  RU-фолбэками в коде; строка описания появляется в лоре кнопок `/rc menu` и в сводке `/rc`.
- **Ready-notify:** при окончании КД ≥ 30 с звучит сигнал и в actionbar приходит
  ««способность» — готова» (`performance.ready-notify.*`).
- **Босс-бар V2:** персональные бары длительных эффектов (≥ 8 с по умолчанию), максимум 2
  одновременно, градиент и символ темы класса, сегментированная шкала, отсчёт секунд;
  пороги и формат тюнингуются через `hud.boss-bar.*`.
- **Хотбар-бинд:** свитки способностей — `/rc bind <1-5>` выдаёт предмет с PDC-меткой,
  ПКМ по свитку в руке = каст (те же проверки, что у команд); свиток чужого класса
  не кастует; материал и фича-флаг в `hotbar-bind.*`.
- **Сообщения в конфиг:** все игровые тексты вынесены в секцию `messages.*` с RU-фолбэками
  в коде — локализация правкой конфига, без пересборки.

### Изменено
- **Маг: тирный реген маны.** Линейный реген (+2/с) заменён на 4 тира по текущему значению
  маны: 3/4/5/6 маны в секунду при `<25 / <50 / <75 / ≥75` соответственно
  (`classes.MAGE.regen-tier-{1..4}`). Надбавка `regen-bonus` пассивки «Пропитанный маной»
  упразднена — пассивка теперь даёт только −15% входящего урона при мане ≥ 50.
- **Воин: тематические иконки** в `/rc menu` (steel_skin → щит, blood_fury → незеритовый меч,
  war_god → золотой шлем); legacy-id (`shield_wall`, `execute`, `avatar_of_war`) оставлены
  для совместимости со старыми КД.

### Операторское
- **×1.5 XP профильным деревьям** — через LP-ноды `auraskills.<skill>.multiplier.50` на
  группах `class_*`; нативный механизм AuraSkills (`check_multiplier_permissions: true`),
  кода нет, двойного начисления нет.

## [1.2.0] - 2026-08-28

### Добавлено
- **Диагностика:** `/rc debug [player]` с правом `raskolclasses.debug` — вывод версии, класса, ресурса, КД, эффектов, уровня скилла, флага HUD.
- **Принятие класса (V1):** титр «⚔ Воин» градиентом темы + партиклы + звук при смене класса через LuckPerms; флаг «раз в сессию».
- **Ребаланс воина:**
  - Стальная кожа (бывш. Щит-стена): −80% урона, 5 с
  - Удар щитом: Slowness II + Blindness r4, 3 с + таунт мобов
  - Кровавое безумие (бывш. Казнь, id `blood_fury`): 4 с возвращает 20% полученного урона агрессору
  - Бог войны (бывш. Аватара войны): Сила II + Сопротивление I, 8 с
- **6 врождённых пассивок** (цифры в `classes.<CLASS>.passives.*`):
  - Воин — Казнь: 20% шанс ×3 урона по цели ≤20% HP; внутр. КД 6 с
  - Охотник — Хищник: +20% урона по целям ≥80% HP
  - Жрец — Благодать: получаемое лечение +15%
  - Маг — Пропитанный маной: мана ≥50 → −15% урона
  - Разбойник — Мастер ядов: 30% Яд I на 2 с; внутр. КД 3 с
  - Разбойник — Садизм: +3 урона в спину; внутр. КД 2 с
- **Фидбек прока пассивок:** тег в actionbar при срабатывании («Казнь ×3!», «Хищник!», «Яд!», «В спину +3!»).

### Изменено
- **Гигиена API:** `getDescription()` → `getPluginMeta()`, `Sound.valueOf` → `Registry.SOUNDS.get` с фолбэком.
- **CI-гейт:** ран красный при любом deprecation-варнинге javac.
- **Сброс ресурса при смене класса:** «ярость воина» не перетекает в «ману мага».

### Исправлено
- Async-фикс титра принятия класса (обёртка в `runTask`).
- NPE-safe проверка первого принятия класса (когда старый класс = null).

# CHANGELOG — RaskolClasses

Формат: [версия] — дата — имя. Секции Added / Changed / Fixed / Removed.

## [1.6.13] — 2026-09-10 · «Самотестирование»

### Added
- **Команда `/rc selftest`** (permission `raskolclasses.debug`): headless-
  самотестирование плагина, PASS/FAIL-список с итоговой строкой. Семь групп
  проверок, ничего не мутирует:
  1. **Формула резистов:** согласованность симулятора (`simulateTaken`)
     с факторами (`physicalFactor`/`magicFactor`) на живом состоянии
     онлайн-игрока; `[SKIP]` если онлайн-игроков нет или игрок в мире из
     `resist.disabled-worlds`;
  2. **Клампы и NaN-гарды:** cap=0 → 0, cap=100 → ≤100, NaN-cap → конечный
     фактор (headless, без мутаций);
  3. **Целостность реестра:** у каждой способности есть кастер (self или
     targeted), targeted-пары полны, нет сиротских кастеров вне DEFAULTS
     (`AbilityRegistry.integrityProblems()`);
  4. **PDC-раундтрип токенов:** ability/spec/install-свитки создаются и
     читаются обратно, пустой предмет не читается как свиток (временные
     предметы в памяти, без мира и без выдачи игрокам);
  5. **Полнота спек-реестра:** у каждого `Spec` есть `SpecDef`;
  6. **Конфиг-валидация:** диапазоны/типы/конечность резистов и урона
     (прогон `ConfigValidator`);
  7. **Fx-каталог:** все имена звуков/партиклов резолвятся.
- **`AbilityRegistry.integrityProblems()`:** список проблем целостности
  реестра (способности без кастеров, targeted без self-кастера, сироты) —
  источник данных для проверки 3.
- Таб-комплит `/rc`: добавлен `selftest` в корневой список, гейт
  `raskolclasses.debug` (как у `debug`/`health`).

### Changed
- Версия в pom/plugin.yml: 1.6.13; артефакт `raskol-classes-1.6.13.jar`.

## [1.6.12] — 2026-09-10 · «Наблюдаемость»

### Added
- **Команда `/rc health`** (permission `raskolclasses.debug`) — сводка
  наблюдаемости для оператора, все строки однострочные:
  - MSPT-снапшот (`Server#getAverageTickTime`, 2 знака) + TPS (первое значение
    `getTPS()`); строка зелёная при MSPT ≤ 50, красная выше;
  - модификаторы резиста: игроков / записей / размер кэша факторов;
  - кулдауны: игроков / записей;
  - инсталляции: активных / глобальный кап;
  - proc-кулдауны Fx: игроков;
  - звуковой бюджет: сброшено звуков с прошлого `/rc health`
    (окно сбрасывается опросом — семантика «с прошлого вызова»);
  - последний purge: N с назад; аптайм плагина.
- **Метрики FxService:** счётчик `droppedSoundsWindow` (инкремент в ветке
  сброса бюджета в `playSound`), `pollDroppedSounds()` (возврат + сброс окна),
  `procVisualCdSize()`.
- **Метрики CooldownManager:** `trackedPlayers()`, `totalEntries()`.
- **Метрики ResistService:** `trackedPlayers()`, `totalModifiers()`,
  `factorCacheSize()`.
- **Отметки RaskolClasses:** `lastPurgeMillis` (обновляется в purge-таске),
  `enabledAtMillis` (на onEnable) + геттеры для `/rc health`.

### Changed
- Таб-комплит `/rc`: добавлен `health` в корневой список, гейт
  `raskolclasses.debug` (как у `debug`).
- **Политика WARNING задокументирована:** однострочные сообщения без
  стектрейсов везде, где стек не является единственным способом найти причину;
  стек остаётся только в SafeStorage SEVERE (коррупт хранилища).
- Версия в pom/plugin.yml: 1.6.12; артефакт `raskol-classes-1.6.12.jar`.

## [1.6.11] — 2026-09-10 · «Анти-эксплойт и sanity»

### Added
- **LOS-проверка AoE сквозь стены (`combat.aoe-los`, дефолт true):**
  `Targeting.hasLineOfSight` трассирует луч из глаз кастера в глаза цели
  через `rayTraceBlocks` (FluidCollisionMode.NEVER). Применяется к
  боевым AoE: frost_nova, arcane_burst, fan_of_knives, спек FROST.
  Стекло/камень/двери блокируют урон; вода/воздух/трава — проходят.
  **Хиляющие AoE (circle_of_prayer, smite-хил) LOS не получают намеренно:**
  саппорт через стену — нормальный геймплей, задокументировано.
  Гейт `combat.aoe-los: false` возвращает старое поведение.
- **NaN/Infinity-гарды в боевой математике:**
  - `CombatService.dealDamage`/`simulateTaken`: `sanitize(DamageProfile)`
    обнуляет некорректные компоненты (NaN, Infinity, отрицательные) до
    применения урона — битый конфиг урона не падает в NaN-урон.
  - `ResistService.clamp` и `physicalFactor`/`magicFactor`: защита от
    NaN-значений, кламп фактора в [0..1], фолбэк на 1.0 (урон не режется)
    при любой некорректности.
  - `ResourceService`: санитаризация `sanitize()` на всех публичных входах
    (add/refund/consume/addHealBonus, тики регена всех классов) —
    битое значение из конфига или хука не может сломать ресурс или унести
    его за границы.
- **ScrollSanitizer — чистка устаревших свитков на join:**
  слушатель `hotbar/ScrollSanitizer` при входе игрока удаляет из инвентаря
  свитки с мёртвыми/неизвестными id (после переименований абилок/спеков/
  инсталляций между сезонами). Игрок получает одно уведомление
  «Удалены устаревшие классовые свитки: N». Работает на join
  (достаточно редко и безопасно для производительности).

### Changed
- **`AbilityRegistry`:** добавлен `exists(String id)` — пробег по всем
  классам в поиске абилки; используется ScrollSanitizer.
- **`SpecToken`:** добавлен `isSpecScroll(ItemStack)` — проверка
  PDC-ключа `spec_ability` для санитизатора.
- **`InstallToken`:** добавлен `isInstallScroll(ItemStack)` — проверка
  PDC-ключа `install` для санитизатора.
- **`MageAbilities.frostNova` / `arcaneBurst`:** цели за стенами
  пропускаются через `Targeting.hasLineOfSight`.
- **`RogueAbilities.fanOfKnives`:** цели за стенами пропускаются.
- **`SpecActiveCaster` спек FROST:** цели за стенами пропускаются.
- Версия в pom/plugin.yml: 1.6.11; артефакт `raskol-classes-1.6.11.jar`.

## [1.6.10] — 2026-09-10 · «Надёжность хранения»

### Added
- **`storage.SafeStorage` — универсальный безопасный сейвер YAML:**
  - `saveAtomic`: пишем в `<file>.tmp` → предыдущую версию уносим в `<file>.bak` →
    атомарно переименовываем tmp в целевой файл. После обесточки/`kill -9` на
    диске не может остаться полубитого yml: есть либо целостный текущий, либо `.bak`.
    Фолбэк на `REPLACE_EXISTING` на ФС, не поддерживающих `ATOMIC_MOVE`.
  - `loadWithFallback`: при битом YAML — WARNING одной строкой и попытка чтения
    `.bak`; если и `.bak` бит/отсутствует — пустая конфигурация + SEVERE в лог
    (инцидент виден оператору, плагин поднимается, данные с нуля).
- **Автосейв хранилищ:** таск в `RaskolClasses` каждые `storage.autosave-minutes`
  (дефолт 5) пишет `cooldowns.yml` и `spec-choices.yml` через SafeStorage.
  При краше теряется не более N минут кулдаунов; спек-выборы не теряются
  вовсе (сейв на каждое изменение уже был, теперь он атомарный).

### Changed
- **`CooldownManager`:** конструктор использует `SafeStorage.loadWithFallback`
  (битый `cooldowns.yml` больше не роняет загрузку молча), `persist()` —
  `SafeStorage.saveAtomic`. Поведение ready-notify и персиста на join/quit
  без изменений.
- **`SpecStorage`:** `load()` на `SafeStorage.loadWithFallback` (битый
  `spec-choices.yml` → `.bak` → пустая конфигурация с SEVERE), `save()` —
  `SafeStorage.saveAtomic`. Сейв на каждое изменение (`set`/`remove`)
  сохранён — выбор и отречение спеки не теряются.
- Версия в pom/plugin.yml: 1.6.10; артефакт `raskol-classes-1.6.10.jar`.

## [1.6.9] — 2026-09-10 · «Баланс-рубильники»

### Added
- **`resist.disabled-worlds`:** список миров, где резисты не применяются вовсе
  (арены, спавн-хабы) — путь A, путь B и симулятор `/rc debug` ведут себя
  согласованно (урон проходит целиком).
- **`resist.pvp-cap`:** отдельный кап суммарного резиста для урона, пришедшего
  от игрока (прямой удар или снаряд с шутером-игроком; в пути B — source-игрок).
  Дефолт равен `resist.cap` → поведение по умолчанию не меняется; значение 0
  полностью выключает резисты в PvP (режим дуэльных арен).
- **`combat.true-damage-cap-per-hit`** (дефолт 1000): предохранитель компоненты
  чистого урона профиля за один вызов — защита от опечаток баланс-конфига и
  fuse для будущего контента 1.7.x; применяется в `dealDamage` и
  `simulateTaken` согласованно.

### Changed
- `ResistService`: breakdown и факторы принимают кап параметром; PvE-кэш
  факторов (на игрока на тик) сохранён, PvP-путь считает по вызову
  (корректность важнее микрооптимизации).
- Версия в pom/plugin.yml: 1.6.9; артефакт `raskol-classes-1.6.9.jar`.

## [1.6.8] — 2026-09-09 · «Края и совместимость»

### Added
- **Килл-кредит владельцу инсталляций:** при срабатывании капкана/руны
  онлайн-владелец передаётся как `source` в `CombatService.dealDamage`:
  дроп с моба, статистика убийств AuraSkills и смерть-лог видят убийство
  ловушкой как убийство владельцем. Оффлайн-владелец → `source = null`,
  урон проходит без кредита (безопасный фолбэк).
- **Spectator/creative-гард в обоих путях урона:** зрители пропускаются
  в пути A (`EntityDamageEvent`) до вычислений; зрители и креативщики
  пропускаются в пути B (`dealDamage`) — ванильная неуязвимость соблюдается
  без лишних вызовов и без рисков флага `SUPPRESS`.
- **Явное перечисление средовых причин в карте типов:** `CRAMMING` и
  `DRYOUT` теперь явно помечены `PHYSICAL` в `DamageType.defaultFor`,
  чтобы их типизация не менялась молча при будущих сменах дефолтов Bukkit.

### Changed
- **MELTING намеренно не трогаем:** константа `DamageCause.MELTING`
  deprecated в новых билдах Paper, событие фактически не возникает.
  CI-гейт по deprecation важнее мёртвой ветки.
- Версия в pom/plugin.yml: 1.6.8; артефакт `raskol-classes-1.6.8.jar`.

### Fixed
- **1.6.8.1:** восстановлен метод `CombatService.simulateTaken`, случайно
  потерянный при замене `CombatService` для 1.6.8 (ссылка из симулятора
  `/rc debug` ломала компиляцию всего модуля).

## [1.6.7] — 2026-09-09 · «Харденинг конфига»

### Added
- **ConfigValidator:** проверка конфига резистов/урона на старте и при
  `/rc reload`: диапазоны 0..100 и конечность (ловит `.nan`/`.inf`) для
  `resist.cap`, `resist.classes.*`, `resist.grants.*`, `resist.mana-soaked.*`,
  `resist.specs.*`; значения `damage-types.vanilla-map.*` из множества
  {physical, magic, true}; `damage-physical`/`damage-magic` способностей и
  инсталляций ≥ 0. Каждое нарушение = WARNING с путём ключа; рантайм-безопасность
  обеспечена клампом итога в ResistService и фолбэками в CombatService.typeOf,
  поэтому битое значение не роняет бой, а подменяется дефолтом/зажимом.
- **Стартовая сводка баз:** строка в лог «Базы резистов (маг/физ):
  WARRIOR 12/27; ROGUE 12/14; MAGE 26/12; PRIEST 30/16; HUNTER 12/16; кап 90».

### Changed
- ClassBook: ключ сообщения маг-гранта переименован в
  `book.resist.grant-magic` (плоский ключ; прежний `grant.magic` был
  недостижим через path-API Bukkit, т.к. ключ `grant` занят строкой физ-гранта).
- Версия в pom/plugin.yml: 1.6.7; артефакт `raskol-classes-1.6.7.jar`.

## [1.6.6] — 2026-09-09 · «Резисты в Книге класса»

### Added
- **Сводка резистов во вкладке «Класс»** (предмет-щит, слот 14): итоги
  физ/маг с базой класса, список активных модификаторов
  («• steel_skin: +15 физ / +0 маг»), строка капа; значения из
  `ResistService.breakdown` — тот же источник, что у `/rc debug`.
- **Grant-строки в лоре:** способности, дающие резист, показывают
  «Даёт: +X% физрезиста на Y с» (Стальная кожа, Уклонение); спека Страж —
  «Пассив: +X% физрезиста постоянно».
- **Секция конфига `resist.grants.*`:** единый источник значений грантов для
  боевого кода и лора Книги (`steel_skin.physical: 15`, `evasion.physical: 30`);
  правка значения + `/rc reload` меняет и лор, и фактический модификатор.
- **Ключи сообщений `messages.book.resist.*`** (title, phys, magic, mod, none,
  cap, grant, spec) — локализация сводки и grant-строк без пересборки.

### Changed
- `WarriorAbilities.steelSkin` и `RogueAbilities.evasion` читают значения
  грантов из конфига вместо хардкода-констант (единый источник с лором).
- Версия в pom/plugin.yml: 1.6.6; артефакт `raskol-classes-1.6.6.jar`.

## [1.6.5] — 2026-09-09 · «PAPI-плейсхолдеры резистов»

### Added
- **Три новых PAPI-плейсхолдера** в экспансии `raskolclasses`:
  - `%raskolclasses_phys_resist%` — итоговый физрезист игрока, целое %
    (0, если класс не выбран);
  - `%raskolclasses_magic_resist%` — итоговый магрезист, целое %;
  - `%raskolclasses_resist_mods%` — список активных модификаторов через
    запятую (например, «steel_skin, guardian»), или «нет» если бафов нет.
- Существующие три плейсхолдера (`class`, `resource`, `resource_max`)
  не тронуты — прошитые в TAB/скорборд/холотемы шаблоны продолжают работать.
- **Рекомендация по применению:** по умолчанию резисты **не выводить**
  в основной TAB-список (информационный шум и раскрытие тактики противнику).
  Целевое применение: персональные скорборды игрока (по его выбору),
  дуэльные арены (отдельный табло-оверлей во время дуэли), holo-табличка
  герольда со статическими базами резистов классов, админ-GUI.

### Changed
- Версия в pom/plugin.yml: 1.6.5; артефакт `raskol-classes-1.6.5.jar`.

## [1.6.4] — 2026-09-09 · «Диагностика урона»

### Added
- **Симулятор урона в `/rc debug`:** блок «Симулятор урона по цели» — профили
  физ 100, маг 100, гибрид 50/50, чистый 100 и значение «дойдёт» с учётом
  резистов цели; баланс тюнится без боя, цифры с одной десятой.
- **Боевой лог `combat.debug-damage`** (дефолт false): при включении каждое
  наше применение урона (путь B) летит в чат онлайн-админам с
  `raskolclasses.debug` серой строкой
  «[dmg] источник → цель: профиль X физ / Y маг / Z чист → дошло W (резисты: …)».
- **Единая формула:** `CombatService.simulateTaken()` — ею пользуются и
  симулятор, и боевой код (`dealDamage`), расхождения дебага с реальностью
  исключены конструктивно.

### Changed
- Версия в pom/plugin.yml: 1.6.4; артефакт `raskol-classes-1.6.4.jar`.

## [1.6.3] — 2026-09-08 · «Производительность»

### Added
- **Кэш резист-факторов на игрока на тик** (`ResistService.factorCache`):
  повторные вызовы `physicalFactor`/`magicFactor` в одном тике не пересчитывают
  breakdown; кэш инвалидируется любым изменением модификаторов
  (add/remove/clear/purge) — каст бафа и удар в том же тике считаются корректно.
- **Протокол замера /spark** в приёмке патча: 60-секундный профиль в замесе
  до и после замены jar, сравнение доли `dev.raskol.classes` и MSPT.

### Changed
- Фильтрация истёкших модификаторов без мутации списка (skip при чтении):
  `removeIf` по CopyOnWriteArrayList больше не дёргается на каждый урон;
  мутация и чистка памяти — только в `purgeExpired()` (purge-таск, 1200 тиков).
- `ManaSoakedService`: полный проход по онлайн-игрокам с `getClassOf` — только
  раз в 100 тиков (5 с) для пополнения множества магов; рабочий тик (20 тиков)
  ходит лишь по `mageIds`; экс-маг теряет модификатор за ~1 с (проверка класса
  по множеству в рабочем тике), оффлайн-чистка — через `ResistService.clear`.
- Версия в pom/plugin.yml: 1.6.3; артефакт `raskol-classes-1.6.3.jar`.

## [1.6.2] — 2026-09-08 · «Справедливость боя»

### Added
- **Фракционно-осознанные таргеты (B4):** хелпер `Targeting` в `combat/`
  определяет союзников через FactionHook (одна непустая фракция = союзники;
  сам себе — всегда союзник). Применяется в AoE-способностях
  (arcane_burst, frost_nova, fan_of_knives) и точечных (cheap_shot):
  союзники не получают урон от наших атак.
- **Фильтр союзников для хила/щитов:** lesser_heal/flash_heal/pw_shield через
  ЛКМ-свиток и circle_of_prayer/smite-хил работают только на себе и союзниках;
  попытка хильнуть врага = «Цель не союзник» без списания ресурса.
- **Гейт `combat.friendly-fire`:** дефолт `false` (союзники неуязвимы,
  хилы только своим); `true` возвращает старое поведение (AoE бьёт всех,
  хилы на кого угодно) — для хардкор-арен и дуэльных зон.
- **frost_nova получила врагов-игроков:** раньше била только мобов,
  теперь — всех, кто не союзник; союзники пропускаются.
- **Сообщения:** `messages.ally.no-hit` («Союзника бить нельзя») и
  `messages.ally.no-heal` («Цель не союзник») — локализация отказов.

### Changed
- Мобы по-прежнему бьются без ограничений (регресс не затронут).
- При отказе от каста (союзник в таргете) ресурс и кулдаун не списываются
  через штатный механизм отмены в AbilityRegistry.
- Версия в pom/plugin.yml: 1.6.2; артефакт `raskol-classes-1.6.2.jar`.

### Fixed
- PriestAbilities: восстановлена структура класса (была повреждена
  при копировании, компиляция ломала весь модуль).

## [1.6.1] — 2026-09-07 · «Жизненный цикл модификаторов»

### Fixed
- **B2:** игрок, сменивший класс с мага, больше не держит модификатор
  «Пропитанный маной» навсегда — `ManaSoakedService` снимает модификатор
  в ветке не-мага (раньше ветка делала `continue` и резист оставался).
- **B3:** ThreadLocal-флаг `SUPPRESS` в `CombatService` сбрасывается в `finally`
  после каждого вызова `damage(...)`: если событие не было создано (цель умерла,
  неуязвима, вызов прерван), флаг не «протекает» на следующий ванильный урон —
  «бесплатных» ударов без резиста больше нет.
- **B1 (hardening):** сверка спек-модификаторов резиста с фактической спекой —
  `SpecService.reconcilePassiveResists()` раз в 20 тиков приводит модификатор
  `guardian` в соответствие со спекой (закрывает админ-смену класса при
  выключенном ready-notify и ручные правки `spec-choices.yml`); `choose()`
  дополнительно снимает модификаторы старой спеки перед установкой новых.

### Changed
- Версия в pom/plugin.yml: 1.6.1; артефакт `raskol-classes-1.6.1.jar`.

## [1.6.0] — 2026-09-07 · «Система урона и сопротивлений»

### Added
- **Четыре вида урона:** физический (оружие, руки, падение, утопление),
  магический (способности, яды, огонь), гибридный (физ+маг компоненты в одном
  профиле), чистый (игнорирует резисты и броню целиком).
- **Система сопротивлений (РЕЗИСТ):** магрезист и физрезист; формула
  «получено = компонент × (1 − резист/100)»; базы по классам:
  Воин 12/27, Разбойник 12/14, Маг 26/12, Жрец 30/16, Охотник 12/16 (маг/физ);
  кап суммарного резиста `resist.cap` (дефолт 90).
- **CombatService:** путь A — ванильный урон режется резистом своего типа
  (карта причин → тип с конфиг-оверрайдами `damage-types.vanilla-map`);
  путь B — наш урон через `dealDamage(target, source, DamageProfile)`:
  физ-компонента проходит броню, маг+чистый — через DamageSource
  minecraft:magic (броню не трогает). Двойного применения резиста нет
  (ThreadLocal-маркер своих вызовов).
- **Модификаторы резиста (пакет 3):** Стальная кожа +15 физ (5 с),
  Уклонение +30 физ (4 с), спек-пассивка Стража +10 физ постоянно
  (переживает релог, снимается респецом), «Пропитанный маной»
  +15 физ/маг при мана ≥ 50 (ManaSoakedService, переключение раз в 1 с).
- **Числа урона в конфиге:** `damage-physical` / `damage-magic` у способностей
  и инсталляций (баланс без пересборки).
- `/rc` — строка «Резисты: физ X% · маг Y%»; `/rc debug` — разбивка
  «база + активные модификаторы + итог».

### Changed
- Урон способностей переведён на профили: маг firebolt = гибрид 30/70,
  frost_nova/arcane_burst = маг; жрец smite = маг; разбойник fan_of_knives/
  cheap_shot = физ; инсталляции bear_trap = физ, frost_rune = маг;
  спек-активки shadowweaver/frost = маг, liquidator = физ.
  Охотник (стрелы = ванильный PROJECTILE) и воин (возврат blood_fury через
  событие) типизируются путём A автоматически.
- «Пропитанный маной»: старый механизм (−15% входящего урона через событие)
  заменён честным резистом; `classes.MAGE.passives.mana_soaked.enabled: false`.
- Описания: Стальная кожа «+15% физрезиста на 5 с», Уклонение
  «+30% физрезиста на 4 с» (вместо «−80% урона» и «100% уклонение»).
- Версия в pom/plugin.yml: 1.6.0; артефакт `raskol-classes-1.6.0.jar`.

## [1.5.10] — 2026-09-08 · «Заморозка ветки 1.5.x»

### Added
- Регресс-матрица README расширена до 25 пунктов (все фичи 1.5.0–1.5.9).
- Секция «Соглашение по версиям» в README: патчи 1.5.x vs 1.6.0 vs хотфиксы.

### Changed
- Ветка 1.5.x заморожена: дальнейшие изменения — только хотфиксы критических
  багов (1.5.10.1+) или перенос в 1.6.0.
- Версия в pom/plugin.yml: 1.5.10; артефакт `raskol-classes-1.5.10.jar`.

## [1.5.9] — 2026-09-08 · «Локализация»

### Added
- **Полная локализация Книги класса:** все строки лора и сообщений (названия
  вкладок, цена/кулдаун/открытие, наличие свитка, подсказки управления, строки
  спек и отречения, правила ресурсов, описания инсталляций и короны) читаются
  из `messages.book.*` конфига. Плейсхолдеры: {cost}, {sec}, {level}, {count},
  {name}, {price}, {resource}, {value}, {ttl}.
- **Локализация инсталляций:** сообщения постановки, всех отказов (уровень,
  лимит игрока, глобальный кап, анти-спам, пустота, граница, спавн, клейм)
  и notify владельцу — из `messages.install.*`. Плейсхолдеры: {level}, {skill},
  {max}, {sec}, {name}, {target}.
- **Локализация гейтов:** сообщения блока creative/AuthMe — из
  `messages.gate.blocked` и `messages.gate.blocked.install`.
- Новые ключи автодописываются в `config.yml` при старте или `/rc reload`
  (addDefault + copyDefaults) — ретекст любого сообщения без пересборки jar.

### Changed
- Версия в pom/plugin.yml: 1.5.9; артефакт `raskol-classes-1.5.9.jar`.

## [1.5.8] — 2026-09-08 · «Совместимость и края»

### Added
- **AuthGate:** неаутентифицированные игроки AuthMe (offline-mode, до /login)
  не пользуют свитки и не кастанут способности. AuthMe дёргается через рефлексию
  (`fr.xephi.authme.api.v3.AuthMeApi#isAuthenticated`) — жёсткой зависимости нет;
  если AuthMe отсутствует или рефлексия сломалась — гейт открыт
  (fail-open + одноразовый warning в лог). Гейт `compat.authme-gate` (дефолт true).
- **Creative-гейт:** способности (слоты 1–6) и инсталляции (слот 7) недоступны
  в creative, кроме `raskolclasses.admin` — админ тестирует свободно.
  Гейт `compat.block-casts-in-creative` (дефолт true).
- **Края инсталляций:** постановка в пустоте и на лимите высоты запрещена всегда;
  за мировой границей — запрет через гейт `installations.deny-outside-border`
  (дефолт true). Гейты применены в одной точке на канал: `AbilityRegistry.castOn`,
  `SpecActiveCaster.tryCast`, `InstallationService.tryPlace` + auth-проверки
  в трёх слушателях свитков.
- `AuthMe` добавлен в softdepend plugin.yml (информационно).

### Changed
- Версия в pom/plugin.yml: 1.5.8; артефакт `raskol-classes-1.5.8.jar`.

## [1.5.7] — 2026-09-08 · «Производительность»

### Added
- **Глобальный звуковой бюджет на тик** `performance.sound-budget-per-tick`
  (дефолт 8): в масс-замесах (десятки проков/триггеров мин в один тик) лишние
  sound-пакеты отбрасываются вместо спама в сеть; партиклы бюджетом не тронуты.
- `FxService.purgeStale()` и `InstallationService.purgeStale()` — чистка
  stale-карт (procVisualCd, lastPlace) консолидирована в общий purge-таск
  (1200 тиков) вместо разрозненных чисток.

### Changed
- **Адаптивный свип инсталляций:** при пустом списке активных инсталляций
  таск (10 тиков) возвращается немедленно — ноль работы на пустом сервере;
  чистка `lastPlace` переехала из sweep в `purgeStale()`.
- Версия в pom/plugin.yml: 1.5.7; артефакт `raskol-classes-1.5.7.jar`.

## [1.5.6] — 2026-09-08 · «Визуальные кулдауны»

### Added
- **Полоска перезарядки на свитках в хотбаре** (ScrollCooldownTask): vanilla
  durability-бар через Paper data-components (MAX_DAMAGE=100, DAMAGE=доля
  оставшегося КД). Красный сразу после каста, плавно светлеет, исчезает
  при готовности. Работает для свитков способностей (1–5) и спеков (6);
  свитки инсталляций без КД. Гейт `hotbar-bind.cooldown-bar` (дефолт true).
- **Proc-теги субтайтлом**: теги проков пассивок (`proc.*.tag`) теперь
  показываются через `Title.subtitle` (жёлтый текст, ~1 с видимости) вместо
  actionbar, который затирался HUD-тиком за 0.5 с. Визуальный кулдаун 3 с
  на каждый прок — анти-спам для частых проков вроде «Хищник».

### Changed
- **HUD actionbar фиксированной длины**: убран список активных кулдаунов
  (braille-спиннер + имена+секунды абилок). Строка теперь всегда одного
  формата: `❬ Энергия ☠ ▰▰▰▰▱▱▱▱▱▱ 30/100 ❭` — не прыгает и не съезжает
  при кастах. Кулдауны видны на свитках в хотбаре.
- Версия в pom/plugin.yml: 1.5.6; артефакт `raskol-classes-1.5.6.jar`.

## [1.5.5] — 2026-09-08 · «Книга: живое состояние»

### Added
- Живое обновление открытой Книги класса: после каста, постановки инсталляции,
  выдачи свитка, выбора спеки и респеца содержимое перерисовывается на месте
  без переоткрытия — кулдауны, глоу и наличие свитка актуальны сразу.
- Строка наличия свитка в лоре способностей, спек и инсталляций:
  «Свиток: в инвентаре (N)» / «Свиток: нет».

### Changed
- Защита от дублей свитков: ПКМ по абилке/спеке/инсталляции не выдаёт второй
  свиток с тем же PDC-id — сообщение «Свиток уже в инвентаре — дубль не выдан».
- Версия в pom/plugin.yml: 1.5.5; артефакт `raskol-classes-1.5.5.jar`.

## [1.5.4] — 2026-09-08 · «Книга класса»

### Added
- **Книга класса `/rc menu`** — всё классовое в одном GUI, три вкладки:
  - «Способности»: ЛКМ по абилке = каст, ПКМ = свиток в хотбар; иконка
    инсталляции (ЛКМ = поставить, ПКМ = свиток постановки); готовые абилки
    светятся enchant-глоу;
  - «Специализации»: ПКМ по спеке = выбор (40+) или свиток активки; кристалл
    «Отречение» с двойным подтверждением (взвод → подтверждение в окне 30 с);
  - «Класс и пассивки»: пассивки с числами, корона и титул.
- Книга открывается со звуком темы класса; активная вкладка подсвечена.

### Changed
- Команды `/rc hud`, `/rc bind`, `/rc respec` **удалены** — их функции живут
  в Книге класса; usage сужен до `/rc [1-7|menu|reload|debug]`.
- HUD виден всегда (тумблер убран; spectator-скип остался).
- VFX-каталог пересмотрен под темы классов (звуки различимы и соответствуют
  описанию): жрец — колокол и аметистовый перезвон (`lesser_heal` BLOCK_BELL_USE,
  `flash_heal`/`pw_shield` BLOCK_AMETHYST_BLOCK_CHIME) вместо «железа воина»;
  `war_god` — ENTITY_LIGHTNING_BOLT_THUNDER; `aimed_shot` — ITEM_CROSSBOW_SHOOT;
  `cheetah_aspect` — ENTITY_CAT_HISS.
- Версия в pom/plugin.yml: 1.5.4; артефакт `raskol-classes-1.5.4.jar`.

### Removed
- `gui/ClassMenu` (старое меню способностей) — заменён Книгой класса.

## [1.5.3] — 2026-09-07 · «Диагностика»

### Added
- Валидация каталога `vfx.*` на старте плагина и при `/rc reload`: WARNING в консоль
  на каждое неизвестное имя звука/партикла с путём ключа (раньше опечатка =
  тихий эффект без следа). Итоговая строка: число проблем или «каталог валиден».
- `/rc debug`: строка «Fx: проблем каталога vfx: N» (зелёная при 0) и список
  эффективных звук/партикл для каждой абилки класса + активки спеки.
- README: регресс-матрица 1.5.x из 16 пунктов (прогон перед заморозкой ветки)
  и заметки разработчику (CI deprecation-гейт, артефакт, политика тегов).

### Changed
- Версия в pom/plugin.yml: 1.5.3; артефакт `raskol-classes-1.5.3.jar`.

### Fixed
- Резолв имён звуков/партиклов: индекс реестра по нормализованному ключу
  (регистр и разделители [._-] игнорируются) — ванильные id с подчёркиваниями
  (`entity.experience_orb.pickup`, `entity.player.hurt_freeze`, `block.note_block.pling`,
  `entity.evoker.cast_spell`, `entity.lightning_bolt.impact`) теперь резолвятся;
  алиасы для переименованных (`block.enchanting_table.use`) и отсутствующих
  на части билдов (`item.armor.equip.iron` → `block.anvil.land`).

## [1.5.2] — 2026-09-07 · «Защита и обратная связь инсталляций»

### Added
- Глобальный кап активных инсталляций `installations.max-global` (дефолт 200):
  карта не утонет в минах на масс-варе.
- Анти-спам постановки: пауза `installations.place-anti-spam-ms` (дефолт 1000)
  на игрока между постановками.
- Уведомления владельцу (actionbar): «⚙ <тип>: сработала на X» и «⚙ <тип>: истекла»;
  гейт `installations.notify-owner` (дефолт true).
- Звуки постановки и растворения по типам: знамя — колокол, капкан — трипвайр,
  вард — маяк, руна — фрост, шашка — шипение.
- `/rc debug`: строка «Инсталляции на сервере: X/max» + список инсталляций цели
  с остатком TTL и координатами.

### Changed
- Версия в pom/plugin.yml: 1.5.2; артефакт `raskol-classes-1.5.2.jar`.

## [1.5.1] — 2026-09-06 · «Целостность»

### Added
- Ready-notify для спек-абилок: actionbar + звук по истечении КД активки спеки
  (гейт `performance.ready-notify.spec-enabled`, дефолт true).
- Точка обрыва в дебаге: `[dbg] обрыв: цель в creative/spectator`.

### Changed
- Гигиена респеца: свитки старой спеки сгорают из инвентаря при отречении
  (сообщение с количеством).
- Версия в pom/plugin.yml: 1.5.1; артефакт `raskol-classes-1.5.1.jar`.

### Fixed
- Guard «спека ≠ класс»: после админ-смены класса старая спека отключается сама
  (storage + LP-нода чистятся, атрибуты снимаются, игрок получает сообщение).
- Таргет-каст больше не тратит ресурс и кулдаун на цели в creative/spectator.
- Память: чистка анти-спам карты `lastAttempts` на quit и в purge-таске;
  чистка состояния ready-notify на quit.
- Хотфикс 1.5.1.1: импорт `SpecToken` после переноса класса в пакет `spec`.
- Хотфикс 1.5.1.2: `SpecBindListener` — `tryCast` возвращает void; убран дубль VFX.

## [1.5.0] — 2026-09-02 · «Инсталляции и чувства»

### Added
- **FxService — движок звука/партиклов (Пакет 1):** единая точка всех VFX/SFX,
  звуки в радиусе ~16 блоков, партиклы с бюджетом. Каталог дефолтов вшит в код;
  конфиг `vfx.*` только переопределяет. Резолв через `Registry.SOUNDS` и
  `Registry.PARTICLE_TYPE` (без deprecated API).
- **Трейлы снарядов (Пакет 1):** фаербол мага с пламенным трейлом + треск огня;
  стрелы охотника с искровым трейлом.
- **Инсталляции (Пакет 2):** ставящиеся мины/варды, слот 7 (`/rc 7`, свиток
  `/rc bind 7`, анлок 50): Знамя войны, Капкан, Световой вард, Ледяная руна,
  Дымовая шашка. Лимит 2 на игрока, TTL 60 с, запрет у спавна и в клеймах Towny.
- **VFX/SFX-пасс активок 1–6 (Пакеты 1+3):** звук каста + партикл на кастере/цели.
- **Фидбек проков пассивок (Пакет 3):** партикл + звук + actionbar-тег
  (`vfx.proc-actionbar: true`): Казнь, Хищник, Благодать, Пропитанный маной,
  Отравленные клинки, Садизм + спек-проки (крит/додж/лифстил).
- **VFX свитковых кастов (Пакет 3):** ПКМ со свитком bind 1–5 играет звук/партиклы.

### Changed
- `/rc` usage: `[1-7|menu|hud|reload|debug|bind|spec|respec]`.
- `/rc` (info) и `/rc debug` показывают счётчик инсталляций (X/2 активных).
- Таб-комплит `/rc bind` включает 7.

### Fixed
- `getMaxHealth()` заменён на `Attribute.MAX_HEALTH` (без deprecated API).
- `ITEM_ARMOR_STAND_PLACE` нет в Paper 1.21.4 — заменён на `BLOCK_WOOD_PLACE`.
- `priest::powerWordShield` вместо выдуманного `pwShield`.
- `AbilityRegistry.loadFromConfig` переписан с `DEFAULTS` вместо несуществующих
  методов `RaskolConfig.abilityIds/Cooldown/Duration/Targeted`.
- ЛКМ-бинды жреца: канал `EntityDamageByEntityEvent` с priority LOW и
  ignoreCancelled=false — лечение/щит работают в столицах под защитой Towny/WG.
- ПКМ со свитком всегда каст в себя (убраны обработчики InteractEntity, уводившие
  хил/щит в цель при прицеле на союзника).

## [1.4.0] — 2026-09-02 · «Специализации и Короны»

### Added
- **Специализации:** две на класс, выбор на 40 уровне профильного скилла
  (`/rc spec`, GUI), одна на всю жизнь; хранение `spec-choices.yml`;
  LP-нода `raskolclasses.spec.<id>` для TAB.
- **Боевая механика спеков:** 10 пассивок (постоянно) и 10 активок
  (слот 6: `/rc 6`); атрибутные бонусы (Страж: +2 брони, Следопыт: +10% скорости).
- **Свитки спеков:** `/rc bind 6` — свиток NETHER_STAR, ПКМ — каст;
  свиток чужой спеки не работает.
- **Визуал ловушки охотника:** ItemDisplay «капкан» (tripwire hook ×2.2)
  на земле + партиклы + звук трипвайра; кольцо снежинок у «Кольца льда»,
  сердечки в «Святилище».
- **Платный респец:** `/rc respec` + подтверждение 30 с; цена 250 + 10×уровень;
  списание через economy-контракт RaskolCore (Vault/Essentials), деньги сжигаются;
  админ (`raskolclasses.admin`) — бесплатно; при недоступной экономике — блок.
- **Королевские вкусы:** титулы классов по короне из паспорта Core
  (Рассвет/Вальрадис), партикл-аура (END_ROD / SOUL_FIRE_FLAME), напоминание
  титула при входе; плейсхолдеры `%raskolcrown_crown% / _title% / _faction%`.

### Fixed
- Paper 1.21.4: атрибуты через `Registry.ATTRIBUTE` (констант GENERIC_* нет);
  `spawnParticle` на не-игроке — только через `World`; порядок аргументов
  `Transformation` = (translation, leftRot, scale, rightRot).
- EconomyHook: ленивый резолв реестра Core (баланс респеца показывал 0).
- Восстановлена регистрация SpecBindListener (бинд свитка спеки).
- «Мороз»: замедление ложится и на мобов, не только на игроков.
- `specs.yml`: самовосстановление из шаблона при побитом файле.
- Гейт уровня: админ-байпас для теста; не блокирует выбор, если AuraSkills
  недоступна.

### Changed
- `/rc` usage: `[1-6|menu|hud|reload|debug|bind|spec|respec]`.
- `/rc` (info) и `/rc debug` показывают корону и титул.

## [1.3.2] и ранее — история разработки

База классов: ресурсы 0–100 с реген-правилами, активки слотов 1–5 (лестница
1/10/25/50/75), свитки 1–5 (ПКМ/ЛКМ), HUD action-bar, босс-бары эффектов,
ready-notify, AuraSkills ×1.5 профильным деревьям, чтение класса из паспорта
RaskolCore (LP — фолбэк).

# CHANGELOG — RaskolClasses

Формат: [версия] — дата — имя. Секции Added / Changed / Fixed.

## [1.5.1] — 2026-09-06 · «Целостность»

### Added
- Ready-notify для спек-абилок: actionbar + звук по истечении КД активки спеки
  (гейт `performance.ready-notify.spec-enabled`, дефолт true).
- Точка обрыва в дебаге: `[dbg] обрыв: цель в creative/spectator`.

### Changed
- Гигиена респеца: свитки старой спеки сгорают из инвентаря при отречении
  (сообщение с количеством).
- Версия в pom/plugin.yml: 1.5.1; артефакт `raskol-classes-1.5.1.jar`.

### Fixed
- Guard «спека ≠ класс»: после админ-смены класса старая спека отключается сама
  (storage + LP-нода чистятся, атрибуты снимаются, игрок получает сообщение).
- Таргет-каст больше не тратит ресурс и кулдаун на цели в creative/spectator.
- Память: чистка анти-спам карты `lastAttempts` на quit и в purge-таске;
  чистка состояния ready-notify на quit.
- Хотфикс 1.5.1.1: импорт `SpecToken` после переноса класса в пакет `spec`.
- Хотфикс 1.5.1.2: `SpecBindListener` — `tryCast` возвращает void; убран дубль VFX.

## [1.5.0] — 2026-09-02 · «Инсталляции и чувства»

### Added
- **FxService — движок звука/партиклов (Пакет 1):** единая точка всех VFX/SFX,
  звуки в радиусе ~16 блоков, партиклы с бюджетом. Каталог дефолтов вшит в код;
  конфиг `vfx.*` только переопределяет. Резолв через `Registry.SOUNDS` и
  `Registry.PARTICLE_TYPE` (без deprecated API).
- **Трейлы снарядов (Пакет 1):** фаербол мага с пламенным трейлом + треск огня;
  стрелы охотника с искровым трейлом.
- **Инсталляции (Пакет 2):** ставящиеся мины/варды, слот 7 (`/rc 7`, свиток
  `/rc bind 7`, анлок 50): Знамя войны, Капкан, Световой вард, Ледяная руна,
  Дымовая шашка. Лимит 2 на игрока, TTL 60 с, запрет у спавна и в клеймах Towny.
- **VFX/SFX-пасс активок 1–6 (Пакеты 1+3):** звук каста + партикл на кастере/цели.
- **Фидбек проков пассивок (Пакет 3):** партикл + звук + actionbar-тег
  (`vfx.proc-actionbar: true`): Казнь, Хищник, Благодать, Пропитанный маной,
  Отравленные клинки, Садизм + спек-проки (крит/додж/лифстил).
- **VFX свитковых кастов (Пакет 3):** ПКМ со свитком bind 1–5 играет звук/партиклы.

### Changed
- `/rc` usage: `[1-7|menu|hud|reload|debug|bind|spec|respec]`.
- `/rc` (info) и `/rc debug` показывают счётчик инсталляций (X/2 активных).
- Таб-комплит `/rc bind` включает 7.

### Fixed
- `getMaxHealth()` заменён на `Attribute.MAX_HEALTH` (без deprecated API).
- `ITEM_ARMOR_STAND_PLACE` нет в Paper 1.21.4 — заменён на `BLOCK_WOOD_PLACE`.
- `priest::powerWordShield` вместо выдуманного `pwShield`.
- `AbilityRegistry.loadFromConfig` переписан с `DEFAULTS` вместо несуществующих
  методов `RaskolConfig.abilityIds/Cooldown/Duration/Targeted`.
- ЛКМ-бинды жреца: канал `EntityDamageByEntityEvent` с priority LOW и
  ignoreCancelled=false — лечение/щит работают в столицах под защитой Towny/WG.
- ПКМ со свитком всегда каст в себя (убраны обработчики InteractEntity, уводившие
  хил/щит в цель при прицеле на союзника).

## [1.4.0] — 2026-09-02 · «Специализации и Короны»

### Added
- **Специализации:** две на класс, выбор на 40 уровне профильного скилла
  (`/rc spec`, GUI), одна на всю жизнь; хранение `spec-choices.yml`;
  LP-нода `raskolclasses.spec.<id>` для TAB.
- **Боевая механика спеков:** 10 пассивок (постоянно) и 10 активок
  (слот 6: `/rc 6`); атрибутные бонусы (Страж: +2 брони, Следопыт: +10% скорости).
- **Свитки спеков:** `/rc bind 6` — свиток NETHER_STAR, ПКМ — каст;
  свиток чужой спеки не работает.
- **Визуал ловушки охотника:** ItemDisplay «капкан» (tripwire hook ×2.2)
  на земле + партиклы + звук трипвайра; кольцо снежинок у «Кольца льда»,
  сердечки в «Святилище».
- **Платный респец:** `/rc respec` + подтверждение 30 с; цена 250 + 10×уровень;
  списание через economy-контракт RaskolCore (Vault/Essentials), деньги сжигаются;
  админ (`raskolclasses.admin`) — бесплатно; при недоступной экономике — блок.
- **Королевские вкусы:** титулы классов по короне из паспорта Core
  (Рассвет/Вальрадис), партикл-аура (END_ROD / SOUL_FIRE_FLAME), напоминание
  титула при входе; плейсхолдеры `%raskolcrown_crown% / _title% / _faction%`.

### Fixed
- Paper 1.21.4: атрибуты через `Registry.ATTRIBUTE` (констант GENERIC_* нет);
  `spawnParticle` на не-игроке — только через `World`; порядок аргументов
  `Transformation` = (translation, leftRot, scale, rightRot).
- EconomyHook: ленивый резолв реестра Core (баланс респеца показывал 0).
- Восстановлена регистрация SpecBindListener (бинд свитка спеки).
- «Мороз»: замедление ложится и на мобов, не только на игроков.
- `specs.yml`: самовосстановление из шаблона при побитом файле.
- Гейт уровня: админ-байпас для теста; не блокирует выбор, если AuraSkills
  недоступна.

### Changed
- `/rc` usage: `[1-6|menu|hud|reload|debug|bind|spec|respec]`.
- `/rc` (info) и `/rc debug` показывают корону и титул.

## [1.3.2] и ранее — история разработки

База классов: ресурсы 0–100 с реген-правилами, активки слотов 1–5 (лестница
1/10/25/50/75), свитки 1–5 (ПКМ/ЛКМ), HUD action-bar, босс-бары эффектов,
ready-notify, AuraSkills ×1.5 профильным деревьям, чтение класса из паспорта
RaskolCore (LP — фолбэк).

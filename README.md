# RaskolClasses — «РАСКОЛ | ДВЕ КОРОНЫ»

Классовый слой сервера: пять классов с ресурсами, активными способностями,
специализациями, инсталляциями и королевскими вкусами.

Рантайм: Paper 1.21.4+ · Java 21 · api-version '1.21'.
Soft-depend (все опциональны): LuckPerms, AuraSkills, PlaceholderAPI, RaskolCore, Towny.

## Классы и ресурсы

| Класс | Ресурс | Реген |
|---|---|---|
| Воин | Ярость | −5/с вне боя; +10 за нанесённый/полученный урон |
| Охотник | Концентрация | +5/с вне боя, 0 в бою |
| Жрец | Свет | +2/с всегда; +5 за событие лечения |
| Маг | Мана | тиры 3/4/5/6 по порогам 25/50/75 |
| Разбойник | Энергия | +10/с |

Способности: 5 на класс (слоты 1–5), лестница 1/10/25/50/75.
Каст: `/rc 1–5`; свитки `/rc bind 1–5` (ПКМ — в себя, ЛКМ по союзнику — цель).

## Специализации (1.4.0)

Выбор на 40 уровне профильного скилла: `/rc spec` → GUI двух спек.
Одна на всю жизнь; смена — платный респец. Активка спеки — слот 6
(`/rc 6`, свиток `/rc bind 6` — капкан с визуалом ловушки на земле).
Пассивки — постоянно, числа в `specs.yml`. Хранение: `spec-choices.yml`;
LP-нода `raskolclasses.spec.<id>` (для TAB).
Guard 1.5.1: после админ-смены класса спека отключается сама (storage + LP-нода).

## Инсталляции (1.5.0)

Ставящиеся мины/варды, слот 7 (`/rc 7`, свиток `/rc bind 7`, анлок 50).
Лимит 2 активных на игрока, глобальный кап `max-global` (1.5.2), TTL 60 с,
анти-спам постановки 1 с (1.5.2), запрет у спавна и в клеймах Towny.
Уведомления владельцу о срабатывании/истечении (1.5.2).

| Класс | Инсталляция | Эффект |
|---|---|---|
| Воин | Знамя войны | Resistance I союзникам 6/8 с |
| Охотник | Капкан | Slowness VI 2 с + 3 урона |
| Жрец | Световой вард | +2 HP/с союзникам 6 с |
| Маг | Ледяная руна | 4 урона + Slowness II 3 с |
| Разбойник | Дымовая шашка | Blind 2 с врагам + Speed I себе 3 с |

## Королевские вкусы (1.4.0)

Титул и партикл-аура по короне из паспорта RaskolCore (Рассвет/Вальрадис).
Один класс выглядит по-разному в двух коронах; конфиг — секция `flavors`.
Плейсхолдеры: `%raskolcrown_crown%`, `%raskolcrown_title%`, `%raskolcrown_faction%`.

## VFX/SFX (1.5.0)

Движок FxService: звук в радиусе ~16 блоков, партиклы, каталог дефолтов в коде.
Конфиг `vfx.*` переопределяет дефолты без пересборки; опечатка = тишина, не краш.
1.5.3: имена `vfx.*` валидируются на старте и `/rc reload` (WARNING на неизвестное);
`/rc debug` показывает эффективные звук/партикл каждой абилки и число проблем.
Трейлы снарядов: фаербол с пламенным трейлом + треск огня; стрелы охотника с искрами.
Фидбек проков пассивок: партикл + звук + actionbar-тег (`vfx.proc-actionbar: true`).

## Респец (1.4.0)

`/rc respec` → цена (250 + 10×уровень) → `/rc respec confirm` (окно 30 с).
Деньги списываются через economy-контракт RaskolCore (Vault/Essentials) и
сжигаются — антиинфляционный sink. Админ (`raskolclasses.admin`) — бесплатно.
1.5.1: свитки старой спеки сгорают из инвентаря при отречении.

## Команды

| Команда | Право | Описание |
|---|---|---|
| `/rc` | — | инфо: класс, уровень, ресурс, корона, титул, спека, инсталляции, абилки, пассивки |
| `/rc 1–7` | — | каст способности (6 — активка спеки, 7 — постановка инсталляции) |
| `/rc menu` | — | GUI способностей |
| `/rc bind 1–7` | — | свиток способности/спеки/инсталляции |
| `/rc spec` | — | выбор спеки (40 уровень, один раз) |
| `/rc respec [confirm]` | — | платный сброс спеки (админ — бесплатно) |
| `/rc hud` | — | HUD вкл/выкл |
| `/rc reload` | raskolclasses.admin | перечитать конфиг (+ валидация vfx) |
| `/rc debug [player]` | raskolclasses.debug | диагностика (включая fx-каталог и инсталляции) |

## Плейсхолдеры (PAPI)

`%raskolclasses_*%` — класс/ресурс/способности (RaskolPlaceholder).
`%raskolcrown_crown% / %raskolcrown_title% / %raskolcrown_faction%` — вкусы корон
(FlavorPlaceholder). Добавь `%raskolcrown_title%` в формат TAB — титул виден всем.

## Конфиги

- `config.yml` — весь баланс: ресурсы классов, абилки, пассивки, цены респеца
  (`spec`), вкусы корон (`flavors`), инсталляции (`installations`), VFX/SFX (`vfx`),
  HUD, ready-notify. Применение: `/rc reload`.
- `specs.yml` — числа пассивок/активок специализаций (самовосстанавливается).
- Данные: `cooldowns.yml`, `spec-choices.yml` (папка плагина; не удалять при обновлении jar).

## Регресс-матрица 1.5.x (прогон перед заморозкой ветки)

| # | Фича | Как проверить | Ожидание |
|---|---|---|---|
| 1 | Абилки 1–5 | `/rc 1–5`, свиток ПКМ | каст, трата ресурса, КД, VFX класса |
| 2 | Точечные (жрец) | свиток ЛКМ по союзнику | хил/щит в цель, урон не проходит |
| 3 | Точечные vs защита | то же в столице | работает под Towny/WG (priority LOW) |
| 4 | Точечные vs gamemode | ЛКМ по цели в creative | каст не тратится, `[dbg]` при debug |
| 5 | Спеки | `/rc spec` на 40+, выбор | пассивка постоянна, `/rc 6` активка, LP-нода |
| 6 | Guard спеки | сменить класс через LP | спека сбрасывается, сообщение, нода снята |
| 7 | Свиток спеки | `/rc bind 6` → ПКМ | активка + VFX; чужая спека — отказ |
| 8 | Респец | `/rc respec` → confirm | сброс, списание, свитки сгорели |
| 9 | Инсталляции | `/rc 7` / свиток 7 | постановка, звук типа, триггер врага, TTL |
| 10 | Лимиты инсталляций | 3-я своя / max-global | отказы с сообщениями |
| 11 | Notify владельца | капкан сработал / истёк | actionbar владельцу + звук растворения |
| 12 | Короны/титулы | `/rc`, `%raskolcrown_title%` | корона и титул по фракции |
| 13 | HUD | всегда | actionbar с ресурсом без тумблера |
| 14 | Ready-notify | КД ≥ 30 с (класс и спека) | actionbar + звук по истечении, без дублей |
| 15 | VFX-валидация | битое имя в `vfx.*` → `/rc reload` | WARNING в консоли с путём ключа |
| 16 | `/rc debug` | на себе | инсталляции с TTL, fx-строки абилок, 0 проблем |

## Заметки разработчику

- CI-гейт: любые deprecated API валят ран — не использовать.
- Артефакт: `raskol-classes-<version>.jar` (finalName = artifactId + version).
- Теги ставим на заморозке минорной ветки (v1.5.10); патчи 1.5.x тегами не сопровождаем.
- История: `CHANGELOG.md`.

## Лицензия

© 2026 hayferdahmer — RASKOL Proprietary License v1.0. See LICENSE.
